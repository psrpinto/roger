[tools]
go = "1.25"

[tasks.install]
alias = "i"
run = [
    "mise install"
]

[tasks.build]
description = "Build"
alias = "b"
run = "go build ."

[tasks.start]
description = "Start"
alias = "s"
run = "go run ."
